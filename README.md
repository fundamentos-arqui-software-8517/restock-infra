# Restock Infra
Repositorio de infraestructura para desplegar el backend, Redis y MongoDB usando Docker Compose.

## Despliegue Local
1. Copia `.env.example` a `.env` y llena los secretos.
2. Ejecuta `docker compose up -d`